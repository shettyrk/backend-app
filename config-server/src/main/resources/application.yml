server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/shettyrk/config-repo
          default-label: main
          clone-on-start: true
          force-pull: true

# OPTIONAL: If your repo is private
#          password: <TOKEN>
